'use strict';

ngDescribe({
    name: 'Navbar',
    module: 'app',
    element: '<qd-navbar></qd-navbar>',
    tests: function(deps) {
        // it('should have a site title', function() {
        //     var logo = deps.element.find('.brand-logo');
            
        //     expect(logo).toBeDefined();
        //     expect(logo.text()).toEqual('QuizDeck');
        // });
    }
});
