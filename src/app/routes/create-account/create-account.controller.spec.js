'use strict';

ngDescribe({
    name: 'Create Account Page',
    module: 'app',
    element: '<div ng-controller="createAccountController as ctrl"></div>',
    tests: function (deps) {
        
    }
});
